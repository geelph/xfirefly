id: web-centerm
info:
  name: centerm
  author: archer
  verified: false
  description: 检测到centerm系统或组件的特征
  created: 2025/04/24
rules:
  r0:
    request:
      method: GET
      path: /
    expression: response.body.ibcontains(b"new ct.extapp.aboutsystemwindow()")
expression: r0()
